name: C++ CI

on:
  push:
  pull_request:

jobs:
  build-test:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release -- -j

      - name: Test
        run: ctest --test-dir build --output-on-failure

      - name: Static checks (warnings as errors)
        run: |
          cmake -S . -B build-strict -DCMAKE_BUILD_TYPE=Release -DINVENTORY_STRICT=ON
          cmake --build build-strict --config Release -- -j VERBOSE=1
